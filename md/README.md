# Edge TTS 测试项目

基于 `https://tts.2068.online` API 的文本转语音（TTS）测试应用。

## 功能特性

- 🎙️ 支持多种语音选择
- 🔍 按语言和性别筛选语音
- 📝 实时文本输入和字符计数
- 🎵 在线播放生成的音频
- 💾 支持音频下载
- 📱 响应式设计，支持移动端

## 项目结构

```
edge-tts-test/
├── api/
│   ├── voices.js          # 获取语音列表API
│   └── speak.js           # 生成语音API
├── public/
│   ├── index.html         # 前端页面
│   ├── styles.css         # 样式文件
│   └── app.js             # 前端逻辑
├── package.json           # 项目依赖
├── vercel.json           # Vercel配置文件
└── README.md             # 项目文档
```

## 本地开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:3000` 查看应用。

## 部署到 Vercel

### 1. 安装 Vercel CLI

```bash
npm install -g vercel
```

### 2. 登录 Vercel

```bash
vercel login
```

### 3. 部署项目

```bash
vercel
```

### 4. 生产环境部署

```bash
vercel --prod
```

## API 端点

### 获取语音列表

```
GET /api/voices
```

响应示例：
```json
[
  {
    "Name": "Microsoft Eric",
    "ShortName": "en-US-EricNeural",
    "Locale": "en-US",
    "Gender": "Male",
    "SampleRateHertz": "24000"
  }
]
```

### 生成语音

```
POST /api/speak
```

请求体：
```json
{
  "text": "要转换的文本内容",
  "voice": {
    "id": "语音ID"
  }
}
```

## 技术栈

- **前端**: HTML5, CSS3, JavaScript (ES6+)
- **后端**: Node.js (Vercel Serverless Functions)
- **部署**: Vercel

## 注意事项

1. 文本输入限制为 1000 字符
2. Voice ID 参数可能无法正确应用，所有生成的语音可能使用相同的默认声音
3. 避免使用 rate、volume、pitch 等可选参数，可能导致 500 错误

## 许可证

MIT
